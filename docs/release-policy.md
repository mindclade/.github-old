# Shared workflow release policy

Consumers pin released shared workflows to an immutable commit or protected release tag according to organization policy. Breaking inputs, outputs, permissions, or required checks require a major workflow-contract release.
